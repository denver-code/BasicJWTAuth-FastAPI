```
$ git clone https://github.com/denver-code/BasicJWTAuth-FastAPI
$ cd BasicJWTAuth-FastAPI
$ python3 -m venv .venv
$ source .venv/bin/activate
$ pip3 install -r requirements.txt
$ uvicorn app:app --reload
```